#include <stdio.h>
#include <stdlib.h>
#include "./../common.h"

struct node *circularUpdateAtIndex(node *start, int index, int payload);
struct node *circularUpdateElement(node *start, int element, int payload);
