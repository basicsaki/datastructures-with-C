#include <stdio.h>
#include <stdlib.h>
#include "./../common.h"

struct node *doublyUpdateAtIndex(node *start, int index, int payload);
struct node *doublyUpdateElement(node *start, int element, int payload);
